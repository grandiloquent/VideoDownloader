# Git

```
git add . && git commit -m "更新" && git push

git init && git add . && git commit -m "first commit" && git branch -M main && git remote add origin git@github.com:grandiloquent/NDK-Camera-Example.git && git push -u origin main

```